# Test Cases

**Test Basis:** Approved Scenarios Artifact — Checkout Card Payment, Promo, and Stock Validation Regression
**Risk Register:** R-01 to R-05 (approved)

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Payment Gateway Authorization | Order confirmed on approved gateway authorization | Cart contains eligible items; checkout page loaded with card payment selected; sandbox card configured to return "approved" | 1. Customer submits card payment 2. Gateway returns authorization result "approved" 3. Observe order status in UI and order record | Order status is set to the "confirmed" value (exact literal string not specified — verify against system's order-status enum); order record shows authorization=approved; no payment-failed flag present | Critical | Functional | Pending |
| TC-002 | Payment Gateway Authorization | Order marked payment-failed on declined gateway authorization | Same as TC-001 but sandbox card configured to return "declined" | 1. Customer submits card payment 2. Gateway returns authorization result "declined" 3. Observe order status | Order status is set to the "payment failed" value (exact literal string not specified — verify against system's order-status enum); no funds are captured; customer sees a payment-failed message at checkout | Critical | Negative | Pending |
| TC-003 | Payment Gateway Authorization | Order set to pending-retry on gateway timeout | Same as TC-001 but sandbox/test gateway configured to simulate a timeout response | 1. Customer submits card payment 2. Gateway returns authorization result "timeout" 3. Observe order status and retry option | Order status is set to the "pending retry" value (exact literal string not specified — verify against system's order-status enum); customer is offered a retry option; no duplicate authorization is created | Critical | Negative | Pending |
| TC-004 | Stock & Cart Lock Consistency | Payment reversal triggered when locked item's last unit sells out before capture | Customer has locked a cart containing the last available unit of an item; payment authorization obtained but capture not yet completed | 1. Simulate depletion of the locked item's last unit before capture completes 2. Attempt to complete payment capture 3. Observe payment authorization state and order status | Payment authorization is reversed (no capture recorded against the order); order status reflects "could not be fulfilled" (exact literal string not specified — verify against system enum); no funds are debited from the customer's account | High | Negative | Pending |
| TC-005 | Card Data Security — Masking | CVV and card number are masked/never exposed during checkout | Customer is on the checkout payment page with a saved or newly entered card; test-environment logging and API response capture are enabled | 1. Customer enters or selects a card and completes the payment step 2. Inspect all API responses returned during checkout 3. Inspect application/server logs for the transaction 4. Inspect the UI display of the card number | CVV does not appear in any UI element, API response payload, or log entry at any checkout step; card number is displayed only in masked form (first 6 and last 4 digits visible, remaining digits masked) | High | Security | Pending |
| TC-006 | Card Data Security — Input Validation | Payment rejected when CVV contains invalid characters | Customer is entering card details at checkout; no prior payment has been submitted for this attempt | 1. Customer enters a valid card number and expiry 2. Customer enters a CVV with non-numeric characters (e.g. "12a") or an oversized value (e.g. 6 digits) 3. Customer submits the payment | Payment submission is rejected before reaching the gateway; customer sees a generic validation message (e.g. "Invalid CVV") containing no stack trace, error code, or system detail | Medium | Negative | Pending |
| TC-007 | Duplicate Capture Prevention | No duplicate capture recorded when card payment is retried after network timeout | Customer's initial card payment attempt experienced a network timeout before confirmation was received; idempotency-key mechanism status is not specified — flagged as an open blocker in the Test Plan | 1. Customer submits card payment; simulate network timeout before confirmation is received 2. Customer retries the identical payment for the same order 3. Inspect transaction/capture records against the order | Exactly one capture record exists against the order; exactly one debit appears against the customer's account; result is treated as provisional pending confirmation of the idempotency-key mechanism (per Test Plan blocker) | High | Negative | Pending |
| TC-008 | Promo & Price Recalculation | Bank debit matches recalculated cart total after a price change with promo applied | Customer has applied a valid promo code to their cart; test environment configured to trigger an item price change before payment capture | 1. Apply the promo code to the cart 2. Trigger a price change on a cart item before capture completes 3. Proceed to payment capture 4. Compare the displayed cart total, order-record total, and actual bank debit amount | Cart total is recalculated to include both the promo discount and the updated item price; the amount debited from the customer's bank account equals this recalculated total exactly, with no discrepancy | Medium | Functional | Pending |
| TC-009 | Promo Eligibility Boundary | Promo rejected when cart total is one unit below the minimum eligible value | Promo code configured with a minimum eligible cart-value threshold (exact numeric value not specified — TBD pending business-rule confirmation); cart total set to one currency unit below that threshold | 1. Customer applies the promo code with cart total one unit below minimum 2. Observe promo application result | Promo discount is rejected; cart total remains unchanged (no discount applied); customer sees a message indicating the cart does not meet the promo's minimum-value requirement | Medium | Boundary | Pending |
| TC-010 | Promo Eligibility Boundary | Promo applied when cart total is exactly at the minimum eligible value | Same threshold configuration as TC-009; cart total set exactly at the minimum threshold | 1. Customer applies the promo code with cart total exactly at minimum 2. Observe promo application result | Promo discount is applied; displayed cart total reflects the discount amount | Medium | Boundary | Pending |
| TC-011 | Promo Eligibility Boundary | Promo applied when cart total is one unit above the minimum eligible value | Same threshold configuration as TC-009; cart total set to one currency unit above the minimum threshold | 1. Customer applies the promo code with cart total one unit above minimum 2. Observe promo application result | Promo discount is applied; displayed cart total reflects the discount amount | Medium | Boundary | Pending |

> **Risk key:** Critical / High / Medium / Low — inherited from the scenario's `@risk-…` tag and the corresponding Risk Register row (R-01…R-05).

---

**Open items carried forward from the Scenarios artifact (not resolved here):**
- TC-001/TC-002/TC-003: literal order-status strings are not confirmed — verify against the system's actual status enum before execution.
- TC-004: literal "could not be fulfilled" status string is likewise not confirmed.
- TC-007: result should be treated as pending until the idempotency-key mechanism is confirmed (Test Plan blocker).
- TC-009/TC-010/TC-011: the actual minimum cart-value threshold is TBD pending promo business-rule confirmation; test data must be parameterized once the real value is known.

*This is a draft artifact for tester review. It has not been approved or signed off.*

---
_Generated by TestFlowAssistant — Stage: Test Cases_
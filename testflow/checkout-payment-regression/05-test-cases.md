# Test Cases

Derived from the Approved Scenarios Artifact (Checkout Payment Regression). Each case traces to a Risk Register row (R-01–R-05); risk bands are inherited from those rows, not re-assessed here.

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|-----------------|------|------|--------|
| TC-001 | Payment Amount Validation | Server-calculated amount matches cart total minus promo discount | Customer has an active session; cart contains items eligible for card checkout; a valid promo code has been applied to the cart; sandbox payment gateway is reachable | 1. Verify cart total reflects the applied discount on the checkout page. 2. Submit payment for the order. 3. Inspect the payment request sent to the sandbox gateway | The amount in the gateway request equals the server-recalculated cart total minus discount; the order confirmation page displays that same charged amount, matching the gateway request amount exactly (no currency/rounding delta) | Critical | Functional | Pending |
| TC-002 | Payment Amount Validation | Tampered client-side amount is rejected before the gateway call | Customer has an active session; cart contains items eligible for checkout; a valid promo code has been applied; sandbox gateway is reachable | 1. Intercept the outgoing payment request and modify the amount field to a value that differs from the server-calculated total. 2. Submit the modified payment request | System returns a validation rejection to the client; the sandbox gateway transaction log shows zero calls for this submission attempt; order status remains "unpaid" | Critical | Negative | Pending |
| TC-003 | Stock & Capture Lifecycle | Capture is blocked when stock depletes between authorization and capture | An item is in stock and payment has been authorized (not yet captured) for that item; order is in "authorized" state | 1. Confirm order is in authorized/pre-capture state. 2. Deplete stock for the authorized item to zero (e.g., via inventory update) before the capture step runs. 3. Trigger the capture process for the order | Capture is blocked, or an automatic reversal is triggered; order status changes to "payment reversed - out of stock"; no additional funds beyond the original authorization hold are captured | Critical | Negative | Pending |
| TC-004 | Duplicate Payment Prevention (Retry) | Duplicate debit is prevented on retry after gateway timeout | A payment request was previously submitted with an idempotency key and the gateway response timed out (no confirmation received) | 1. Confirm no confirmed capture exists yet for the original request (timeout state). 2. Retry the payment submission using the identical idempotency key | Backend transaction log shows exactly one capture record for the order; the retry response returns the original transaction result/ID; no second charge is created | High | Negative | Pending |
| TC-005 | Promo Code Input Validation | Promo code field rejects SQL-injection-pattern input | Customer is on the checkout page with items in the cart; promo code field is visible and enabled | 1. Enter `' OR '1'='1` into the promo code field. 2. Submit the promo code | System displays the generic error "Invalid promo code"; the HTTP response body contains no backend/database error detail (no stack trace, no query fragment) | High | Negative | Pending |
| TC-006 | Promo Code Input Validation | Promo code field rejects script-injection (XSS) input | Customer is on the checkout page with items in the cart; promo code field is visible and enabled | 1. Enter `<script>alert(1)</script>` into the promo code field. 2. Submit the promo code | System displays the generic error "Invalid promo code"; the script is not reflected or executed in the rendered page or response body; no backend error detail is exposed | High | Negative | Pending |
| TC-007 | Promo Code Input Validation | Promo code exceeding maximum allowed length is rejected | Customer is on the checkout page with items in the cart; promo code field is visible and enabled. **Note:** the exact maximum character length is not specified in the test basis — TBD pending a defined limit | 1. Enter a promo code string exceeding the (TBD) maximum allowed character length. 2. Submit the promo code | System rejects the input with a length-specific validation error (not a generic system/server error); order is not able to proceed to payment with the oversized value applied | High | Boundary | Pending |
| TC-008 | Card Data Security (Logging) | Card data is masked in logs and responses on a failed payment | Customer has entered card payment details at checkout; sandbox gateway is configured to return a decline for the test card used | 1. Submit the card payment details for checkout. 2. Trigger a gateway decline. 3. Inspect the application/error logs and the HTTP error response returned to the client | Logs contain only masked card data (first six and last four digits visible, remainder masked); the error response body contains no raw CVV and no full card number | Medium | Negative | Pending |

> **Risk key:** Critical / High / Medium / Low — inherited from Risk Register rows R-01–R-05.

---

**Notes on coverage and open items**

- TC-007's boundary length is marked TBD — no numeric limit was specified in the test basis (per the scenarios artifact's own note); this must be resolved before the case can be executed with a concrete value.
- TC-001–TC-008 cover R-01 through R-05 with at least one negative test case per functional area (Payment Amount Validation, Stock & Capture Lifecycle, Duplicate Payment Prevention, Promo Code Input Validation, Card Data Security), satisfying the negative-coverage requirement.
- Per the scenarios artifact, cases tracing to scenarios blocked on ENT-2/ENT-3 (sandbox credentials, server-vs-client promo validation model) are drafted but cannot be executed until those entry criteria close.
- BNPL, wallet, net banking, refund/chargeback, and multi-currency flows are out of scope, consistent with the test plan.
- This artifact is a draft for tester review and requires human approval before execution.

---

_Generated by TestFlowAssistant — Stage: Test Cases_
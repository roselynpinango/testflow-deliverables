# Test Cases

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Payment Capture / Promo | Captured amount matches recalculated cart total after promo application | Cart contains item(s) with a known total prior to promo application; checkout page loaded with card payment selected; a valid promo code is available for the account | 1. Record cart total before promo application 2. Apply the valid promo code 3. Submit payment with valid card details | Captured/authorized amount recorded by the gateway equals the recalculated total (original total minus discount); order confirmation screen displays the identical recalculated total; no mismatch between capture record and displayed total | Critical | Functional | Pending |
| TC-002 | Payment Retry / Idempotency | Exactly one authorization recorded when retrying payment after timeout using same idempotency key | Cart has an item ready for checkout; checkout page loaded with card payment selected; test environment can simulate a submission timeout; an idempotency key is generated for the first submission attempt | 1. Submit payment and simulate a timeout with no confirmed response received 2. Retry the payment submission reusing the same idempotency key 3. Query transaction records/account statement for this order | Exactly one authorization/capture record exists for the transaction under that idempotency key; no second (duplicate) debit entry appears in the customer's transaction log or statement | Critical | Functional | Pending |
| TC-003 | Order State / Capture Callback | Order remains pending when capture callback does not arrive | An authorization has been successfully initiated for an order; test environment can simulate non-delivery/failure of the capture callback from the gateway | 1. Submit payment; confirm authorization succeeds 2. Simulate the capture callback failing to arrive 3. Check the order status in the order system | Order status remains "Pending" (not "Confirmed"); no order confirmation notification (email/UI message) is sent to the customer | High | Functional | Pending |
| TC-004 | Payment Capture / Stock Boundary | Payment capture blocked when stock reaches zero at moment of submission | Item stock quantity = 1 (last unit) in the cart; checkout page loaded with card payment selected; test environment can trigger stock depletion concurrently with submission | 1. Trigger stock quantity to reach 0 at the same instant payment submission is processed 2. Submit payment | Payment capture is blocked and does not execute; order status is set to "Unconfirmed – pending stock re-validation"; no debit is recorded against the customer's account | High | Boundary | Pending |
| TC-005 | Card Data Security | CVV and card number are masked in UI, API response, and logs | Customer is on checkout page with card payment fields; valid card number and CVV are entered | 1. Enter card number and CVV 2. Submit payment for authorization 3. Inspect the UI confirmation screen, the API response payload, and the application/server logs for this transaction | CVV value is absent from UI, API response, and logs in all three locations; card number appears only in masked form (first 6 + last 4 digits, e.g., 411111******1111) in UI, API response, and logs | High | Security | Pending |
| TC-006 | Promo Code Input Validation | Promo code field rejects oversized and script-injection input without applying discount or logging raw value | Checkout page loaded; promo code field is empty and available for input | 1. Enter an oversized string (length exceeding the field's documented limit) as the promo code and submit 2. Enter a script-injection pattern (e.g., `<script>alert(1)</script>`) as the promo code and submit | Both submissions are rejected with a validation error message; cart total remains unchanged (no discount applied) after either attempt; application logs do not contain the raw oversized or injected string (only a sanitized/rejection entry, if any) | High | Negative | Pending |
| TC-007 | Promo Code Validation | Malformed promo code is rejected and charged amount is unaffected | Cart has a valid item and an original total; checkout page loaded; no promo code applied yet | 1. Enter a malformed promo code (e.g., invalid format string) 2. Attempt to apply it | Promo code is rejected with a validation message displayed on the checkout page; amount charged at payment submission equals the original cart total (no discount applied) | Medium | Negative | Pending |
| TC-008 | Promo Code Stacking | Second promo code is rejected when one promo is already applied | Cart already has one valid promo code applied, yielding a discounted total (recorded value) | 1. Attempt to apply a second promo code ("STACK2") on top of the already-applied discount 2. Submit payment | Second promo code is rejected with a message indicating stacking is not allowed; charged amount equals the previously discounted total, unchanged by the second attempt | Medium | Negative | Pending |
| TC-009 | Promo Code Expiry | Expired promo code is rejected and full cart total is charged | Cart has a valid item and original (undiscounted) total; promo code "EXPIRED10" exists in the system with an expiry date in the past | 1. Enter the expired promo code "EXPIRED10" 2. Attempt to apply it 3. Submit payment | Promo code is rejected with an expiry-specific validation message; charged amount equals the full original cart total with no discount applied | Medium | Negative | Pending |

> **Risk key:** Critical / High / Medium / Low — inherited from the Approved Risk Register (R-01…R-05) via the source scenario's risk tag.

---

**Notes (not part of the reviewable artifact content, for tester awareness):**
- TC-008 and TC-009 derive from the two Examples rows of the "Promo code stacking and expiry" Scenario Outline; kept as separate cases since they exercise distinct equivalence partitions (already-stacked vs. expired) rather than duplicating one condition.
- Open items carried forward from Scenarios stage remain unresolved here: 3DS/OTP applicability (TBD), sandbox/test bank credentials blocker (EN-2), and EC-4 coverage target (unmeasured). No case above assumes 3DS behavior or claims coverage against EC-4.
- This artifact is a draft for tester review and has not been approved.
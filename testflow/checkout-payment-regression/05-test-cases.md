# Test Cases

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|------------------|------|------|--------|
| TC-001 | Checkout Payment — Authorization Accuracy | Authorized amount reflects recalculated cart total after promo code application | Customer has an in-stock item in cart; saved card selected as payment method; payment gateway sandbox available; a valid promo code exists that alters the cart total | 1. Add in-stock item to cart. 2. Apply valid promo code and note the recalculated cart total displayed at checkout. 3. Submit card payment for authorization using the saved card. | The authorization request sent to the gateway carries an amount equal to the recalculated cart total (displayed total = authorized amount, zero variance); no amount-mismatch entry is recorded between the cart display value and the gateway authorization request value. | Critical | Functional |
| TC-002 | Order/Payment State Consistency | Order is not confirmed when payment capture succeeds but stock reservation fails | Customer's order has a successful payment authorization; payment capture is ready to be triggered; stock reservation service is available for simulated failure | 1. Trigger payment capture for the order (capture succeeds). 2. Simulate a stock reservation failure for the ordered item. 3. Observe order status and payment ledger. | Order status remains in a non-confirmed state (not "confirmed"); a reversal transaction record is created referencing the original capture ID, and the captured funds are reversed. | Critical | E2E |
| TC-003 | Payment Capture / Stock Validation | Payment capture is blocked when item stock drops to zero before capture | Customer's order has a successful payment authorization with the item in stock at cart display time; item stock quantity is reduced to zero before capture is triggered | 1. Authorize payment for the order while item stock > 0. 2. Reduce item stock to 0 before capture is initiated. 3. Attempt to capture payment. | Payment capture request is rejected (capture status = "blocked"/"failed"; no funds captured); the customer is shown a stock-unavailable message on the checkout/order screen. | High | Negative |
| TC-004 | Gateway Webhook Integration | Checkout system accepts valid gateway callback (HTTP 200, valid schema) and updates order status | An order exists in "authorized" state awaiting a gateway callback; gateway sandbox is configured to send the callback | 1. Gateway sends callback with HTTP status 200 and a payload conforming to the valid schema. 2. Checkout system processes the callback. | Callback is accepted; order status is updated to reflect the callback outcome (e.g., "captured") in the order record; no error is logged. | High | Integration |
| TC-005 | Gateway Webhook Integration | Checkout system rejects malformed gateway callback payload and logs schema validation error | An order exists in "authorized" state awaiting a gateway callback; gateway sandbox configured to send a malformed-schema payload | 1. Gateway sends callback with HTTP status 200 and a payload that does not conform to the expected schema. 2. Checkout system processes the callback. | Payload is rejected and order status remains unchanged; a schema-validation-error log entry referencing the malformed callback is recorded; no order state transition occurs. | High | Negative |
| TC-006 | Gateway Webhook Integration | Checkout system retries callback processing on gateway HTTP 500 response | An order exists in "authorized" state awaiting a gateway callback; gateway sandbox configured to return HTTP 500 with a valid-schema payload | 1. Gateway sends callback with HTTP status 500 and a valid-schema payload. 2. Checkout system processes the callback. | A retry attempt of callback processing is logged/executed per the integration contract; order status remains in a non-final state (not prematurely confirmed) pending retry outcome. Exact retry count/backoff interval: not specified in test basis (TBD). | High | Integration |
| TC-007 | Stock Validation Boundary | Payment capture succeeds and stock decrements to zero when exactly one unit remains at capture time | Ordered item stock quantity is exactly 1 unit at the moment capture is triggered; payment authorization has already succeeded for the order | 1. Set item stock quantity to exactly 1 unit. 2. Trigger payment capture for the order. 3. Observe capture result and updated stock quantity. | Payment capture completes with status "success"; item stock quantity is decremented to 0 in the inventory record. | High | Boundary |
| TC-008 | Security — Data Exposure | CVV is not present in logs or error response when gateway returns a payment error | Customer has entered valid card number, expiry, and CVV for a card payment; payment gateway sandbox is configured to return an error response for this transaction | 1. Submit card payment with card number, expiry, and CVV. 2. Force/simulate a gateway error response. 3. Inspect application logs and the error message shown to the customer. | CVV value does not appear in any log entry or in the error response/UI text; any displayed card number shows only first 6 and last 4 digits, remaining digits masked. | Medium | Security |
| TC-009 | Security — Input Validation | SQL injection payload in promo code field is rejected without exposing system internals | Customer is on the checkout page with an in-stock item in cart; promo code input field is available | 1. Enter a SQL injection payload (e.g., `' OR '1'='1`) into the promo code field. 2. Submit the checkout form. | System rejects the promo code as invalid, displaying a generic "invalid promo code" message; no database error message or stack trace appears in the UI or API response. | Medium | Negative |

> **Risk column key (overrides template placeholder):** Critical / High / Medium / Low — inherited from the scenario's `@risk-…` tag and the corresponding Risk Register row (R-01…R-05). All cases have Status = **Pending** (no execution/run evidence exists yet).

**Traceability**
- TC-001 → R-01 (scenario 1) · TC-002 → R-02 (scenario 2) · TC-003 → R-03 (scenario 3) · TC-004/TC-005/TC-006 → R-04 (webhook outline examples) · TC-007 → R-03 (boundary scenario) · TC-008/TC-009 → R-05 (security scenarios).
- Every functional area covered here (authorization amount accuracy, order/payment state, capture-time stock validation, webhook contract, security/input validation) has at least one negative case (TC-003, TC-005, TC-009), satisfying the ≥1-negative-per-area requirement.
- R-03's concurrency/race-condition sub-risk remains out of scope per the Scenarios artifact's Blockers note — not covered by TC-003/TC-007, and not fabricated here.
- Coverage-target (EC-1) and retry-contract specifics (TC-006) remain **TBD** — not measured/specified in the supplied test basis.

This is a draft artifact for the Cases stage; it requires tester review and approval before use as an authoritative test basis.

_Generated by TestFlowAssistant — Stage: Test Cases_
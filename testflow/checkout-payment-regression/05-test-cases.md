# Test Cases

**Stage:** Test Cases (draft, pending tester review)
**Derived from:** Approved Scenarios Artifact — Checkout Payment Regression — Card Payment, Promo Discount, and Stock Validation
**Risk bands:** inherited from Approved Risk Register (R-01…R-05)

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Functional Suitability — Promo Discount / Payment Gateway | Gateway capture amount equals cart total minus single promo discount | Cart contains items eligible for card payment; a known cart total (pre-discount) is established; payment gateway sandbox environment is available and reachable | 1. Add items to cart so the pre-discount total is a known value. 2. Apply one valid promo code with a fixed discount amount. 3. Complete card payment for the discounted order via the sandbox gateway. | The payment gateway capture amount recorded for the transaction equals (cart total − promo discount amount) exactly, verified against the gateway transaction record for this order. | Critical | Functional | Pending |
| TC-002 | Functional Suitability — Promo Discount (percentage type) | Gateway capture amount reflects correct discount for a percentage-type promo | Cart contains items eligible for card payment; a known cart total is established; a percentage-type promo code is available in the sandbox; **TBD — no sandbox percentage rate or expected discounted total was supplied in the test basis; tester must populate before execution** | 1. Apply a promo code of type "percentage" at checkout. 2. Complete card payment for the discounted order. 3. Retrieve the payment gateway capture amount for the transaction. | The payment gateway capture amount equals the expected discounted total for the applied percentage rate. **Expected numeric value: TBD — not specified; do not execute until sandbox test data is supplied.** | Critical | Functional | Pending |
| TC-003 | Functional Suitability — Promo Discount (fixed-amount type) | Gateway capture amount reflects correct discount for a fixed-amount promo | Cart contains items eligible for card payment; a known cart total is established; a fixed-amount promo code is available in the sandbox; **TBD — no sandbox fixed discount value or expected discounted total was supplied in the test basis; tester must populate before execution** | 1. Apply a promo code of type "fixed_amount" at checkout. 2. Complete card payment for the discounted order. 3. Retrieve the payment gateway capture amount for the transaction. | The payment gateway capture amount equals the expected discounted total for the applied fixed discount. **Expected numeric value: TBD — not specified; do not execute until sandbox test data is supplied.** | Critical | Functional | Pending |
| TC-004 | Functional Suitability — Promo Discount (stacked type) | Gateway capture amount reflects correct discount for stacked promos | Cart contains items eligible for card payment; a known cart total is established; a stacked promo configuration is available in the sandbox; **TBD — no sandbox stacking rule or expected discounted total was supplied in the test basis; tester must populate before execution** | 1. Apply a promo code of type "stacked" at checkout. 2. Complete card payment for the discounted order. 3. Retrieve the payment gateway capture amount for the transaction. | The payment gateway capture amount equals the expected discounted total for the stacked promo combination. **Expected numeric value: TBD — not specified; do not execute until sandbox test data is supplied.** | Critical | Functional | Pending |
| TC-005 | Reliability — Stock/Payment State Consistency | Payment is not captured and order reflects stock conflict when stock is exhausted before capture | Customer has reached the payment step with one item reserved in stock; payment gateway sandbox environment is available | 1. Reach the payment step with the item's stock reservation active. 2. Trigger exhaustion of the reserved stock for that item before payment capture completes. 3. Observe the resulting payment and order status. | No capture transaction is recorded at the payment gateway for this attempt; the order status is set to a stock-conflict state (e.g., "stock unavailable") and is distinct from any "confirmed"/"paid" status value. | High | Integration | Pending |
| TC-006 | Security — Card Data Non-Exposure on Decline | Raw card number and CVV are not exposed in error message or transaction log after a declined payment | Customer is at checkout with a test card configured to be declined in the sandbox; payment gateway sandbox logging is accessible for verification | 1. Submit a card payment using the decline-configured test card. 2. Observe the checkout page error message displayed to the customer. 3. Inspect the payment gateway transaction log entry for this attempt. | The displayed error message contains no full raw card number and no CVV value. The gateway transaction log shows the card number only in masked first-six/last-four format, with no CVV value present anywhere in the log entry. | High | Negative | Pending |
| TC-007 | Reliability — Idempotent Retry / Duplicate-Debit Prevention | Exactly one debit is recorded when a payment retry reuses the same idempotency key after a gateway timeout | Customer submits a card payment that will experience a simulated gateway timeout; the original idempotency key generated for the first attempt is available for reuse | 1. Submit a card payment for the order; simulate a gateway timeout before a response is returned. 2. Retry the same payment submission using the original idempotency key. 3. Query the card/bank statement (sandbox) and merchant transaction records for debit entries against this order. | Exactly one debit entry is recorded against the customer's card for the order; no second/duplicate charge appears in the transaction records or bank statement for the retried request. | High | Regression | Pending |
| TC-008 | Security — Input Validation (Injection) | Promo code field rejects a SQL/script injection input without exposing internal details | Customer is at the promo code entry step during checkout; checkout session is active | 1. Enter a promo code value containing a SQL or script injection pattern (e.g., a quote-based SQL fragment or an inline script tag). 2. Submit the promo code. 3. Observe the resulting UI response. | The promo code is rejected with a generic validation message; no internal system detail, query fragment, or stack trace is displayed; the payment is not processed as a result of this submission. | Medium | Negative | Pending |
| TC-009 | Functional Suitability / Security — Input Validation (Boundary) | Promo code field rejects an oversized input value | Customer is at the promo code entry step during checkout; checkout session is active; **maximum accepted promo code length: not specified in test basis — tester to confirm the boundary value before execution** | 1. Enter a promo code value exceeding the maximum accepted length. 2. Submit the promo code. 3. Observe the resulting UI response. | The promo code is rejected; the checkout page displays a validation message; no payment gateway call is initiated as a result of this submission. | Medium | Boundary | Pending |

> **Risk key (per Standards Emphasis override):** Critical / High / Medium / Low — inherited from the scenario's `@risk-…` tag / risk register row it covers.

---

**Open items carried forward (not resolved, flagged per ISO/IEC 42001 transparency requirement):**
- TC-002, TC-003, TC-004: expected discounted-total values are **TBD** — no sandbox promo rates or cart totals were supplied in the test basis. These cases are not executable until real sandbox test data is provided; do not substitute a plausible guess.
- TC-009: maximum accepted promo code length is **not specified** in the test basis — tester must confirm the exact boundary value before execution.

**Coverage check against quality gate:**
- Negative test cases present: TC-006, TC-008 (≥1 negative per functional area ✔)
- Boundary test case present: TC-009 ✔
- Security scenarios present: TC-006, TC-008 (Security quality gate satisfied ✔)
- Explicit preconditions per case ✔ · Observable/verifiable expected results per case ✔ · Risk band assigned per case, inherited from risk register ✔

_Generated by TestFlowAssistant — Stage: Test Cases (draft, pending tester review — not an approval)._
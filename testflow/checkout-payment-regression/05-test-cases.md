# Test Cases

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Payment Amount Computation | Gateway amount reflects combined promo discount and stock-adjusted price | Cart contains a stock-limited item; active promo code applied; stock-adjusted price already computed; sandbox test bank credentials configured | 1. Add stock-limited item to cart with active promo code applied. 2. Proceed to checkout and submit payment using sandbox test card. 3. Inspect amount transmitted to payment gateway and order confirmation display. | Amount sent to the payment gateway equals cart total minus promo discount, adjusted for stock-limited item price; order confirmation displays this exact charged amount with no discrepancy. | Critical | Functional | Pending |
| TC-002 | Order-Payment State Consistency | Order remains unconfirmed when stock depletes between authorization and capture | Payment has been authorized for the last remaining unit of an item; capture has not yet completed | 1. Authorize payment for the last unit in stock. 2. Deplete stock to zero (e.g., concurrent purchase) before capture completes. 3. Attempt to proceed with capture. | Payment capture does not proceed; the authorization is reversed; order status remains "not confirmed"; no confirmed order record exists against the uncaptured payment. | High | Integration | Pending |
| TC-003 | Security - Data Masking | CVV is never exposed when a stock-out error occurs mid-payment | Customer has entered card details including CVV at checkout; a stock-out condition can be triggered mid-payment | 1. Enter card number, expiry, and CVV at the payment step. 2. Trigger a stock-out error mid-payment submission. 3. Inspect the UI response, error message, and application log for the transaction. | CVV value does not appear in the UI, error message, or application log; displayed card number is masked showing only first 6 and last 4 digits. | Medium | Negative | Pending |
| TC-004 | Payment Amount Computation | Charged amount reflects promo discount only when stock is sufficient | Cart contains item(s) with sufficient stock (no stock-adjusted pricing in effect); an active promo code is available | 1. Add item(s) with sufficient stock to cart. 2. Apply the active promo code. 3. Complete checkout payment. | Amount sent to the payment gateway equals cart total minus promo discount only, with no stock-adjustment applied. | Critical | Functional | Pending |
| TC-005 | Payment Amount Computation | Charged amount reflects stock-adjusted price only when no promo is applied | Cart contains the last remaining unit of a stock-limited item; no promo code is applied | 1. Add the last remaining unit of the stock-limited item to cart. 2. Do not apply any promo code. 3. Complete checkout payment. | Amount sent to the payment gateway equals the stock-adjusted item price only, with no promo discount applied. | Critical | Functional | Pending |
| TC-006 | Input Validation - Promo Code | Oversized promo code input is safely rejected | Promo code field has a defined maximum accepted length (exact value not specified — TBD) | 1. Navigate to the promo code field at checkout. 2. Enter a promo code string exceeding the defined maximum length. 3. Submit the promo code. | Checkout rejects the input with a generic validation message; no stack trace or internal system detail is exposed in the UI or response. | Medium | Boundary | Pending |
| TC-007 | Input Validation - Card Field | Injection-pattern input on the card number field is safely rejected | Customer is on the payment details step of checkout with an empty card number field | 1. Enter a known script/SQL injection pattern into the card number field. 2. Submit the payment form. 3. Inspect the rendered response and downstream processing. | Checkout rejects the input with a generic error message; no injected script executes and no injected query pattern appears in the response or is passed downstream. | Medium | Negative | Pending |
| TC-008 | Promo Code Logic | Expired promo code is rejected and full cart total is charged | A promo code exists that expired before the current date | 1. Enter the expired promo code at checkout. 2. Apply the code. 3. Observe checkout display and complete payment. | Checkout displays an invalid/expired message; the full (undiscounted) cart total is the amount charged. | Medium | Functional | Pending |
| TC-009 | Promo Code Logic | Only one discount is applied when two promo codes are stacked | Two independently valid, single-use promo codes are available for entry | 1. Enter the first valid promo code and apply it. 2. Enter the second valid promo code and apply it. 3. Complete checkout payment. | Only one single-use discount is applied to the charged amount; the second code's discount is not stacked or added. | Medium | Functional | Pending |
| TC-010 | Promo Code Logic | Non-existent promo code is rejected and full cart total is charged | Customer enters a promo code string that does not correspond to any existing code | 1. Enter a non-existent promo code at checkout. 2. Apply the code. 3. Observe checkout display and complete payment. | Checkout displays an invalid code message; the full (undiscounted) cart total is the amount charged. | Medium | Negative | Pending |

> **Risk key:** Critical = must-test this sprint · High = test this sprint if capacity allows · Medium = test if time allows · Low = defer to regression

**Traceability:** TC-001, TC-004, TC-005 → R-01 (critical) · TC-002 → R-02 (high) · TC-003 → R-03 (medium) · TC-006, TC-007 → R-04 (medium) · TC-008, TC-009, TC-010 → R-05 (medium)

**Coverage check:** ≥1 negative case per functional area ✔ (TC-003, TC-007, TC-010) · ≥1 security case ✔ (TC-003, TC-007) · ≥1 boundary case ✔ (TC-006) · Explicit preconditions per case ✔ · Observable/specific expected results ✔

**Open items carried forward (unresolved, owner TBD):**
- Promo stacking + last-item pricing interaction rule
- Sandbox/test bank credential and promo code availability for execution
- Exact maximum accepted length for promo code field (TC-006) — not specified in test basis

---

_Generated by TestFlowAssistant — Stage: Test Cases. Draft for tester review; not an approved or authoritative artifact until a human signs off._
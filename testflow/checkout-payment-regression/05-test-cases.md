# Test Cases

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Functional suitability / Reliability — Order-Stock consistency (R-01) | Order is not confirmed when stock depletes during in-progress authorization | Cart contains the last available unit of Product X; checkout session is valid; sandbox test card is configured; a mechanism exists to trigger a concurrent order against the same SKU | 1. Add the last available unit of Product X to the cart. 2. Submit payment authorization with the valid sandbox test card. 3. Trigger a concurrent order that depletes stock for Product X before capture completes. 4. Query the order status and the gateway authorization outcome. | Order status is not "confirmed"; order status matches the gateway's authorization outcome, both reporting the payment as not captured | Critical | Negative |Pending|
| TC-002 | Reliability — Payment amount accuracy (R-02) | Captured amount matches discounted cart total after promo recalculation | A valid, active promo code exists that reduces the cart total to a discounted amount; checkout session is valid; sandbox test card is configured | 1. Apply the valid promo code to the cart. 2. Confirm the discounted cart total displayed. 3. Submit payment authorization and complete capture. | Gateway authorization/capture amount equals the discounted cart total exactly (amount field in gateway response matches displayed total to the currency's smallest unit) | High | Functional |Pending|
| TC-003 | Reliability / Boundary — Sub-unit rounding (R-02) | Captured amount is correctly rounded when promo discount produces a fractional sub-unit amount | Promo discount applied to cart produces a total with more decimal places than the currency's smallest unit (e.g., 3 decimals for a 2-decimal currency); checkout session valid | 1. Apply the promo code producing the fractional sub-unit total. 2. Note the displayed cart total (rounded to smallest unit per display rule). 3. Submit payment authorization. | Captured amount equals the displayed cart total rounded to the currency's smallest unit; difference between captured amount and displayed total is exactly 0 | High | Boundary |Pending|
| TC-004 | Security — CVV / card data exposure (R-03) | CVV and card number are not exposed when a card payment is declined | Sandbox gateway is configured with a test card set to auto-decline authorization; checkout session is valid | 1. Submit a card payment using the decline-configured test card. 2. Capture the full API/error response returned to the client. 3. Inspect application logs generated for this transaction. | Client response contains no raw CVV value in any field; any card number present in the response or in logs is masked to first 6 and last 4 digits only, with no other unmasked PAN digits present | High | Negative |Pending|
| TC-005 | Compatibility — Webhook idempotency (R-04) | Duplicate webhook callback does not alter a confirmed order's status | An order exists whose status is already "confirmed" following one successfully processed payment webhook with payment reference {{payment_ref}}; no further status transitions have been recorded since | 1. Resend the identical webhook callback with the same payment reference {{payment_ref}}. 2. Query the current order status. 3. Query the order's status-transition log/history. | Order status remains "confirmed"; the status-transition log shows no additional transition entry — transition count is unchanged from before the redelivery | High | Negative |Pending|
| TC-006 | Functional suitability — Promo validation (R-05) | Expired promo code is rejected and cart total unchanged | Promo code "EXPIRED2023" exists and is in an expired state; cart has an established pre-promo total; checkout session valid | 1. Enter promo code "EXPIRED2023" at checkout. 2. Apply the promo code to the cart. | System response text is "promo code expired"; cart total remains exactly equal to the pre-promo amount | Medium | Negative |Pending|
| TC-007 | Functional suitability — Promo validation / input handling (R-05) | Malformed promo code is rejected with invalid-format message | Cart has an established pre-promo total; checkout session valid; no promo code named "ABC#$%123" is required to exist | 1. Enter promo code "ABC#$%123" at checkout. 2. Apply the promo code to the cart. | System response text is "promo code invalid format"; cart total remains exactly equal to the pre-promo amount; no server error/stack trace is returned | Medium | Negative |Pending|
| TC-008 | Functional suitability — Promo validation (R-05) | Non-existent promo code is rejected as not recognized | Promo code "NOTEXIST99" does not exist in the system; cart has an established pre-promo total; checkout session valid | 1. Enter promo code "NOTEXIST99" at checkout. 2. Apply the promo code to the cart. | System response text is "promo code not recognized"; cart total remains exactly equal to the pre-promo amount | Medium | Negative |Pending|

> **Risk key:** Critical / High / Medium / Low — inherited from the scenario's `@risk-…` tag / risk register (R-01…R-05).

---

**Draft for tester review — not approved.** Coverage: TC-001 traces to R-01, TC-002/TC-003 to R-02, TC-004 to R-03, TC-005 to R-04, TC-006–TC-008 to R-05. TC-006/007/008 split the promo `Scenario Outline` into distinct equivalence-partition cases (expired / malformed / unrecognized) rather than one generic case, since each carries a distinct expected message and each is independently exercisable. Coverage target (EC-4) remains **TBD** per the test plan. No cases were generated for BNPL/UPI/wallet, refund/chargeback, or saved-instrument flows — consistent with the approved scenarios' stated out-of-scope note. A human reviewer must confirm risk-band assignments and priorities before this artifact is used for execution or sign-off.

_Generated by TestFlowAssistant — Stage: Test Cases_
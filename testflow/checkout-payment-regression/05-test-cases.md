# Test Cases

_Draft artifact for tester review — Stage: Test Cases. Not approved or authoritative until a human tester signs off._

---

| ID | Area | Title | Preconditions | Steps | Expected Result | Risk | Type | Status |
|----|------|-------|---------------|-------|----------------|------|------|--------|
| TC-001 | Payment Authorization & Order Status | Order confirmed after successful authorization and capture | Customer has a cart ready for checkout; payment gateway configured with sandbox test bank credentials; valid test card available | 1. Submit payment using a valid test card at checkout 2. Gateway returns successful authorization and capture response (sandbox) 3. Query order-service API for order status; check database order status | order-service API returns status "confirmed"; database order status column = "confirmed" | Critical | Functional | Pending |
| TC-002 | Payment Authorization & Order Status | Order marked "payment failed" when gateway declines authorization | Customer has a cart ready for checkout; sandbox test card configured to trigger a decline is available | 1. Submit payment using the decline-triggering test card 2. Gateway returns a decline response 3. Query order-service API for order status; check database order status | order-service API returns status "payment failed"; database order status column = "payment failed"; no capture recorded for the order | Critical | Negative | Pending |
| TC-003 | CVV & Card Data Protection | CVV displayed as masked characters after field loses focus | Customer is on the checkout page with the card payment form displayed | 1. Enter a valid-format CVV into the CVV field 2. Move focus away from the CVV field (e.g., tab to next field) 3. Inspect the rendered CVV field value in the UI/DOM | CVV field displays only masked characters (e.g., •••); raw CVV digits are not present anywhere in the rendered DOM value | High | Functional | Pending |
| TC-004 | CVV & Card Data Protection | Checkout rejects payment submission with invalid CVV format | Customer is on the checkout page with the card payment form; network monitoring/proxy available to confirm outbound requests | 1. Enter a CVV value with an invalid format (e.g., 2 digits or alphabetic characters) 2. Submit the payment form 3. Observe UI validation message and monitor outbound network requests to the gateway | Checkout displays a validation error requesting a valid CVV; zero outbound requests to the gateway are observed in the network monitor | High | Negative | Pending |
| TC-005 | CVV & Card Data Protection | CVV absent from network logs and error-message payloads across failure types | Tooling can capture network log entries and error-message payloads; sandbox can trigger each failure type on demand | 1. Trigger a payment attempt resulting in "invalid CVV"; capture network log entry and error-message payload 2. Trigger a payment attempt resulting in "gateway timeout"; capture the same artifacts 3. Trigger a payment attempt resulting in "gateway decline"; capture the same artifacts | For all three failure types, a string search for the submitted CVV value in the captured network log entry and error-message payload returns zero matches | High | Regression | Pending |
| TC-006 | Idempotent Payment Retry | Duplicate debit prevented when identical payment request is retried with the same idempotency key after timeout | Customer initiates payment with a generated idempotency key; sandbox configured to simulate a gateway timeout on the first attempt | 1. Submit the initial payment request; simulate a timeout before a gateway response is received 2. Resend the identical payment request using the same idempotency key 3. Query order-service for capture records and the customer account for debit records | Exactly one capture is recorded for the order; exactly one debit is recorded against the customer's account | High | Functional | Pending |
| TC-007 | Idempotent Payment Retry | Payment request without an idempotency key is rejected before reaching the gateway | Payment request can be submitted with the idempotency key field omitted; network monitoring available | 1. Submit a payment request with no idempotency key present 2. Observe the API response 3. Monitor outbound calls to the payment gateway | API response returns a validation error identifying the missing idempotency key; zero authorization calls are sent to the gateway | High | Negative | Pending |
| TC-008 | Stock Validation During Checkout | Authorization rolled back when stock depletes to zero before capture | Product listed at checkout has a tracked stock level > 0; a concurrent sandbox transaction can be triggered to deplete stock to zero | 1. Customer submits payment for the product; authorization is initiated and left in-flight 2. Trigger a concurrent sandbox transaction that depletes stock to zero while authorization is in-flight 3. Allow checkout to attempt proceeding to capture; query order status | Capture is blocked and the order is rolled back before capture completes; order status field = "stock unavailable" | Medium | Functional | Pending |
| TC-009 | Stock Validation During Checkout | Customer sees out-of-stock error after concurrent stock depletion, no authorization initiated | Product stock has already been depleted to zero by a concurrent transaction before the customer's checkout attempt | 1. Customer attempts to complete checkout for the depleted product 2. Observe checkout UI response 3. Monitor outbound calls to the payment gateway | Checkout displays an out-of-stock error message; zero authorization requests are sent to the gateway | Medium | Negative | Pending |
| TC-010 | Stock Validation During Checkout | Two concurrent checkouts contend for the last unit of stock | Product stock level is exactly 1 unit; two customer sessions are ready to submit checkout simultaneously | 1. Set product stock level to exactly 1 unit 2. Trigger checkout submission from two customer sessions at the same time 3. Observe which session proceeds to payment authorization and which is blocked | Exactly one checkout session proceeds to payment authorization; the other session is shown an out-of-stock error before any authorization is initiated for it | Medium | Boundary | Pending |
| TC-011 | Promo-Adjusted Amount Consistency | Authorization and settlement amounts match cart total across promo types (percentage, flat, expired) | Customer has a cart with a promo code applied; sandbox supports percentage, flat, and expired promo test data | 1. Apply a "percentage" promo code; authorize and settle payment; record cart total, authorization amount, settlement amount 2. Apply a "flat" promo code; repeat and record the same three values 3. Apply an "expired" promo code; repeat and record the same three values | For percentage and flat: authorization amount = discounted cart total and settlement amount = discounted cart total. For expired: authorization amount = original undiscounted cart total and settlement amount = original undiscounted cart total | Medium | Regression | Pending |
| TC-012 | Promo-Adjusted Amount Consistency | Expired promo code is rejected and cart total reverts to original price | Customer has a cart with an expired promo code entered but not yet re-evaluated | 1. Apply the expired promo code to the cart 2. Trigger cart re-evaluation (e.g., page refresh or proceed to checkout) 3. Observe cart error message and displayed cart total | Cart displays an expired-promo error message; displayed cart total equals the original, undiscounted price (matches pre-promo cart total value) | Medium | Negative | Pending |

> **Risk key (overrides P1/P2/P3 placeholder):** Risk band is inherited from the source scenario's `@risk-…` tag / risk register row: Critical, High, Medium, Low.

---

**Traceability note:** Each test case maps 1:1 (or, for data-driven scenarios TC-005 and TC-011, aggregates the scenario's Examples table into one non-duplicative case) to a scenario in the approved Scenarios artifact, which in turn maps to a risk-register row (R-01 → TC-001/002, R-02 → TC-003/004/005, R-03 → TC-006/007, R-04 → TC-008/009/010, R-05 → TC-011/012).

**Coverage check:** Negative case present for every functional area (TC-002, TC-004, TC-007, TC-009, TC-012). Security coverage present (TC-003, TC-004, TC-005) satisfying the regulated-industry gate for this scope; no additional authentication/authorization/session-management scenarios were present in the approved Scenarios artifact to derive cases from — flagged as **not specified** rather than invented.

_Generated by TestFlowAssistant — Stage: Test Cases_